<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ splash screen icon: a centered, tightly bounded version of
     the brand logo. The layer-list gives the system a clear intrinsic size
     and safe area so the icon never overflows or fails to load. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:width="192dp"
        android:height="192dp"
        android:gravity="center">
        <bitmap
            android:src="@drawable/splash_logo"
            android:gravity="center" />
    </item>
</layer-list>
